services:
  phoenixd:
    build:
      context: .
    restart: unless-stopped
    ports:
      - "9740:9740"
    volumes:
      - ./phoenix-data:/phoenix/.phoenix
      - ./satdress-data:/phoenix/.satdress